{
  "name": "ablespace-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --workspace=frontend",
    "dev": "npm run dev --workspace=frontend"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
